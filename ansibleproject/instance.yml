---
- hosts: server
  tasks:
     - name: install git 
       yum:
        name: git
        state: present
     - name: install apache server 
       yum: 
        name: httpd
        state: present
     - name: Copy file with owner and permissions
       ansible.builtin.copy:
        src: /test
        dest: /Desktop/test
        owner:  root


